const JoinForm = () => {
  return (
    <form>
      <input placeholder="id" />
      <input placeholder="password" />
      <button>카카오 계정으로 신규 가입</button>
      <button>깃헙 계정으로 신규 가입</button>
    </form>
  );
};

export default JoinForm;
